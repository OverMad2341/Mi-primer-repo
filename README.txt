* git pull = Extraer todo lo que tengas en la nube

* git branch --all = muestra toda las ramas

* git checkout nombre-de-la-rama = Moverse entre ramas 

* git merge nombre-de-la-rama = copiar la rama propuesta en la rama actual

* git status = para ver los seguimientos

* git add . = confirmar los cambios (el punto acepta todos)

* git push origin nombre-de-la-rama = subir los cambios a la nube

* git add controller/app... (agregar los archivos por ruta o archivo)

* git commit -m "mensaje" (hacer commit).

* git push origin nombredelarama (hacer push de local a remoto)

* git branch -d <branch> (Borra una branch local)

* git remote update (actualiza los repositorios) 

* git branch nombredelarama (Crear un branch)

* git checkout -b rama (Crear branch desde alguna rama).

* git checkout nombredelarama (CAMBIAR DE BRANCH)

* git rebase --continue (si hay conflictos y se arreglaron hay que ejecutar esto)

* git log (ver los log)  y git log -p para ver los detalles de los cambios.
 
* git pull origin nombredelarama (traer todos los cambios).

* git remote update (para actualizar las ramas en el local)

* touch nombre-del-archivo.html (para crear un archivo nuevo)